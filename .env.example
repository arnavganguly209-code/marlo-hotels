# Public site URL (used for canonical URLs, sitemap, OpenGraph, Secure cookies)
# Production MUST be https://your-domain — cookies and auth depend on this.
NEXT_PUBLIC_SITE_URL=https://marlo.theglobalorbit.com

# PostgreSQL connection string (Prisma) — required for CMS metrics/content,
# NOT required for Orbit login itself.
# Example: postgresql://marlo:password@localhost:5432/marlo_hotels?schema=public
DATABASE_URL=

# Orbit administration (server-only — never prefix these with NEXT_PUBLIC_)
# Both must be present in the PM2 / systemd runtime environment.
ORBIT_ADMIN_PASSKEY=
# Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
ORBIT_SESSION_SECRET=

# Persistent media path on the VPS (required in production).
# Local default: <project>/public/media
# Production example: /srv/marlo-media
ORBIT_UPLOAD_DIR=

# ---------------------------------------------------------------------------
# Booking confirmation email (SMTP — server-only)
# Webmail: https://webmail.theglobalorbit.com
# SMTP hostname must match the mail server TLS certificate SANs.
# NEVER commit a real SMTP_PASSWORD. Set it only on the production server.
# Never use NEXT_PUBLIC_ for any of these.
# ---------------------------------------------------------------------------
SMTP_HOST=mail.theglobalorbit.com
SMTP_PORT=587
SMTP_ENCRYPTION=tls
SMTP_USER=booking@marlohotels.com
SMTP_FROM=booking@marlohotels.com
BOOKING_NOTIFICATION_EMAIL=booking@marlohotels.com
# Set on the VPS only (PM2 / .env) — leave empty in git:
SMTP_PASSWORD=

# ---------------------------------------------------------------------------
# PayPal Checkout (REST Orders API — server-only secret)
# Use sandbox until Live credentials are explicitly authorized.
# NEVER commit PAYPAL_CLIENT_SECRET. Never use NEXT_PUBLIC_ for the secret.
# Client ID is exposed to the browser only via /api/paypal/client-config.
# ---------------------------------------------------------------------------
PAYPAL_ENVIRONMENT=sandbox
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
